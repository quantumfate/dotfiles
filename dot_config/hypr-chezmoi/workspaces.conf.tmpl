workspace=1, monitor:{{ .primary_monitor }}, persistent:true, default:true{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ else }}, layoutopt:mfact:0.5{{ end }}
workspace=2, monitor:{{ .primary_monitor }}, persistent:true{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ else }}, layoutopt:mfact:0.5{{ end }}
workspace=3, monitor:{{ .primary_monitor }}, persistent:true{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ else }}, layoutopt:mfact:0.5{{ end }}
workspace=4, monitor:{{ .primary_monitor }}, persistent:true{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ else }}, layoutopt:mfact:0.5{{ end }}
workspace=5, monitor:{{ .primary_monitor }}, persistent:true, gapsin:0, gapsout:0, bordersize:0{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ else }}, layoutopt:mfact:0.5{{ end }}
workspace=6, monitor:{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ .secondary_monitor }}{{ else }}{{ .primary_monitor }}{{ end }}, persistent:true, layoutopt:rientation:right, layoutopt:mfact:0.5
workspace=7, monitor:{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ .secondary_monitor }}{{ else }}{{ .primary_monitor }}{{ end }}, persistent:true, layoutopt:rientation:right, layoutopt:mfact:0.5
workspace=8, monitor:{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ .secondary_monitor }}{{ else }}{{ .primary_monitor }}{{ end }}, persistent:true, layoutopt:rientation:right, layoutopt:mfact:0.5
workspace=9, monitor:{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ .secondary_monitor }}{{ else }}{{ .primary_monitor }}{{ end }}, persistent:true, layoutopt:rientation:right, layoutopt:mfact:0.5
workspace=10, monitor:{{ if and (hasKey . "secondary_monitor") .secondary_monitor }}{{ .secondary_monitor }}{{ else }}{{ .primary_monitor }}{{ end }}, persistent:true, layoutopt:orientation:right, layoutopt:mfact:0.5
